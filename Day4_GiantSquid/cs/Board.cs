
using cs;

class Board {
	static uint BSize => Program.BoardSize;

	static readonly UInt16[,] board = new UInt16[BSize,BSize];
	static readonly bool[,] marks = new bool[BSize,BSize];

	public void SetRow(uint row, List<uint> nums) {
		
	}
}